# Frontend Admin

React admin panel for products, categories, orders, and users.

```bash
cd frontend_admin
npm install
copy .env.example .env
npm start
```

Open:

```txt
http://localhost:3001
```

Because frontend_user normally uses port 3000, run admin with:

```bash
set PORT=3001 && npm start
```
